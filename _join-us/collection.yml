collections:
  join-us:
    output: true
    order:
      - As Member.md
      - As Observer (Judiciary).md
      - As Observer (Individual Judge)/As Observer (Individual Judge.md
      - As Observer (Individual Judge)/.keep
